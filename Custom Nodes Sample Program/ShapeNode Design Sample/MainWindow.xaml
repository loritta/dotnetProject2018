<Window x:Class="ShapeDesign.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:diag="clr-namespace:MindFusion.Diagramming.Wpf;assembly=MindFusion.Diagramming.Wpf"
    Title="MindFusion.Diagramming Sample: Shape Designer" Height="768" Width="1024" Icon="/App.ico">
	<DockPanel Name="dock">
		<Menu DockPanel.Dock="Top">
			<MenuItem Header="File">
				<MenuItem Header="New library" Click="OnFileNew" />
				<MenuItem Header="Save library" Click="OnFileSave" />
				<MenuItem Header="Load library" Click="OnFileOpen" />
			</MenuItem>
			<MenuItem Header="Shapes">
				<MenuItem Header="New" Click="OnShapesNew" />
				<MenuItem Header="Remove" Click="OnShapesRemove" />
			</MenuItem>
		</Menu>
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition />
				<RowDefinition Height="150" />
			</Grid.RowDefinitions>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>
			<diag:NodeListView x:Name="nodeList" />
			<diag:ShapeDesigner x:Name="designer" ShapeChanged="OnShapeChanged" Grid.Column="1" />
			<diag:Diagram x:Name="diagram" Grid.Row="1" Grid.Column="1" />
		</Grid>
	</DockPanel>
</Window>
