<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:ProjectDiagramV1">

    <Style TargetType="local:CFAttributeNode">
        <Setter Property="Template">
            <Setter.Value>
                <DataTemplate DataType="local:CFAttributeNode">

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="25"></RowDefinition>
                        </Grid.RowDefinitions>

                        <TextBox AcceptsReturn="False" FontFamily="Verdana" FontSize="12" 
                                 Foreground="Black" Text="{Binding AttributeName}" 
                                 MaxLength="30" TextAlignment="Left" Padding="25,0,0,0"
                                 VerticalAlignment="Center">

                        </TextBox>
                    </Grid>

                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>